import React from 'react';
import { Waveform } from '@uiball/loaders';

export default function Loading() {
  return (
    <Waveform
      size={80}
      lineWeight={6}
      speed={1}
      color="#BEADFA"
    />
  );
}
